{
  "name": "La-Divina-Commedia",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build:site": "node scripts/build.mjs",
    "build:search": "pagefind --site dist",
    "build": "npm run build:site && npm run build:search",
    "dev": "npm run build && serve dist",
    "dev:drafts": "SHOW_DRAFTS=1 npm run build && serve dist"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0",
    "serve": "^14.2.1"
  }
}
